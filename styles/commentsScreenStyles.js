import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#FFF',
    },
    commentsContainer: {
        marginTop: 10,
        marginLeft: 'auto',
        marginRight: 'auto',
        marginBottom: 10,
    },
    searchImage: {
        marginLeft: 'auto',
        marginRight: 'auto',
        marginBottom: 30,

        width: 343,
        height: 50
    }
})

export default styles;